[package]
name = "artop-validation"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "AUTOSAR business constraints layer (port of C++ AutosarConstraints, aligned to org.artop.aal.*.constraints)."

[dependencies]
emf-common = { workspace = true }
emf-validation = { workspace = true }

[dev-dependencies]
emf-ecore = { workspace = true }

[lints.rust]