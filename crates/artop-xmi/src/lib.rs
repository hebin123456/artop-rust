//! XMI/XML serialization (port of C++ `emf-xmi`).
//!
//! Port target: C++ `emf-xmi` module of `hebin123456/artop-cpp`.
//!
//! This file is *skeleton*: each module below is a compile placeholder that
//! will be filled with the port of the corresponding C++ translation unit.
//! Filled by GitHub Actions; see `.github/workflows/ci.yml`.

pub mod xml_base_handler {
    //! Port target: C++ source unit for `xml_base_handler`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::xml_base_handler"
    }
}

pub mod sax_mi_handler {
    //! Port target: C++ source unit for `sax_mi_handler`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::sax_mi_handler"
    }
}

pub mod sax_xml_handler {
    //! Port target: C++ source unit for `sax_xml_handler`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::sax_xml_handler"
    }
}

pub mod xmi_handler {
    //! Port target: C++ source unit for `xmi_handler`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::xmi_handler"
    }
}

pub mod xmi_helper {
    //! Port target: C++ source unit for `xmi_helper`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::xmi_helper"
    }
}

pub mod xmi_loader {
    //! Port target: C++ source unit for `xmi_loader`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::xmi_loader"
    }
}

pub mod xmi_resource {
    //! Port target: C++ source unit for `xmi_resource`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::xmi_resource"
    }
}

pub mod xmi_resource_factory {
    //! Port target: C++ source unit for `xmi_resource_factory`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::xmi_resource_factory"
    }
}

pub mod xmi_saver {
    //! Port target: C++ source unit for `xmi_saver`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::xmi_saver"
    }
}

pub mod xml_handler {
    //! Port target: C++ source unit for `xml_handler`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::xml_handler"
    }
}

pub mod xml_helper {
    //! Port target: C++ source unit for `xml_helper`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::xml_helper"
    }
}

pub mod xml_load_impl {
    //! Port target: C++ source unit for `xml_load_impl`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::xml_load_impl"
    }
}

pub mod xml_save_impl {
    //! Port target: C++ source unit for `xml_save_impl`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-xmi::xml_save_impl"
    }
}

#[cfg(test)]
mod tests {
    #[test]
    fn skeleton_compiles() {
        assert_eq!(
            super::xml_base_handler::api_surface(),
            "artop-xmi::xml_base_handler"
        );
    }
}
