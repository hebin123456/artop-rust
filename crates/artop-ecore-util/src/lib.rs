//! Extra Ecore utility layer (port of C++ `emf-ecore-util`).
//!
//! Port target: C++ `emf-ecore-util` module of `hebin123456/artop-cpp`.
//!
//! This file is *skeleton*: each module below is a compile placeholder that
//! will be filled with the port of the corresponding C++ translation unit.
//! Filled by GitHub Actions; see `.github/workflows/ci.yml`.

pub mod extended_metadata {
    //! Port target: C++ source unit for `extended_metadata`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::extended_metadata"
    }
}

pub mod feature_map {
    //! Port target: C++ source unit for `feature_map`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::feature_map"
    }
}

pub mod conversion_delegate {
    //! Port target: C++ source unit for `conversion_delegate`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::conversion_delegate"
    }
}

pub mod copier {
    //! Port target: C++ source unit for `copier`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::copier"
    }
}

pub mod e_contents_elist {
    //! Port target: C++ source unit for `e_contents_elist`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::e_contents_elist"
    }
}

pub mod e_cross_reference_adapter {
    //! Port target: C++ source unit for `e_cross_reference_adapter`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::e_cross_reference_adapter"
    }
}

pub mod eobject_containment_elist {
    //! Port target: C++ source unit for `eobject_containment_elist`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::eobject_containment_elist"
    }
}

pub mod eobject_containment_inverse_elist {
    //! Port target: C++ source unit for `eobject_containment_inverse_elist`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::eobject_containment_inverse_elist"
    }
}

pub mod eobject_elist {
    //! Port target: C++ source unit for `eobject_elist`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::eobject_elist"
    }
}

pub mod eobject_resolving_elist {
    //! Port target: C++ source unit for `eobject_resolving_elist`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::eobject_resolving_elist"
    }
}

pub mod eobject_validator {
    //! Port target: C++ source unit for `eobject_validator`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::eobject_validator"
    }
}

pub mod eobject_inverse_elist {
    //! Port target: C++ source unit for `eobject_inverse_elist`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::eobject_inverse_elist"
    }
}

pub mod eobject_inverse_resolving_elist {
    //! Port target: C++ source unit for `eobject_inverse_resolving_elist`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::eobject_inverse_resolving_elist"
    }
}

pub mod validator_registry {
    //! Port target: C++ source unit for `validator_registry`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::validator_registry"
    }
}

pub mod ecore_adapter_factory {
    //! Port target: C++ source unit for `ecore_adapter_factory`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::ecore_adapter_factory"
    }
}

pub mod ecore_emap {
    //! Port target: C++ source unit for `ecore_emap`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::ecore_emap"
    }
}

pub mod ecore_switch {
    //! Port target: C++ source unit for `ecore_switch`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::ecore_switch"
    }
}

pub mod ecore_util {
    //! Port target: C++ source unit for `ecore_util`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::ecore_util"
    }
}

pub mod ecore_validator {
    //! Port target: C++ source unit for `ecore_validator`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::ecore_validator"
    }
}

pub mod equality_helper {
    //! Port target: C++ source unit for `equality_helper`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::equality_helper"
    }
}

pub mod feature_map_util {
    //! Port target: C++ source unit for `feature_map_util`.
    /// Placeholder marker so the module compiles until the real port lands.
    pub fn api_surface() -> &'static str {
        "artop-ecore-util::feature_map_util"
    }
}

#[cfg(test)]
mod tests {
    #[test]
    fn skeleton_compiles() {
        assert_eq!(
            super::extended_metadata::api_surface(),
            "artop-ecore-util::extended_metadata"
        );
    }
}
